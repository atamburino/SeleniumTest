using System;
using System.Threading.Tasks;

class Program
{
    static async Task Main()
    {
        // Selenium tests are now xUnit tests and run with `dotnet test`.
        // You can add any other manual entrypoint code here if needed.
    }
}
